(()=>{const c=supabase.createClient(VOLTTECH_SUPABASE.url,VOLTTECH_SUPABASE.publishableKey),q=s=>document.querySelector(s),x=v=>String(v??"").replace(/[&<>"']/g,a=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[a]));let rows=[],names=new Map;async function load(){const{data:{session}}=await c.auth.getSession();if(!session){location.href="account.html?returnTo=%2Fadmin-deletions.html";return}const{data:ok}=await c.rpc("is_volttech_admin");if(ok!==true){location.replace("account.html");return}const[{data,error},{data:customers}]=await Promise.all([c.from("account_deletion_requests").select("*").in("status",["requested","processing"]).order("requested_at",{ascending:true}),c.rpc("admin_customers")]);names=new Map((customers||[]).map(v=>[v.id,v]));if(error){q("#status").textContent=error.message;return}rows=data||[];q("#requests").innerHTML=rows.length?rows.map(card).join(""):'<p class="empty">No deletion requests are waiting.</p>';document.querySelectorAll("[data-finalize]").forEach(b=>b.onclick=()=>finalize(b.dataset.finalize))}function card(r){const p=names.get(r.user_id)||{},due=new Date(r.recovery_until),ready=Date.now()>=due.getTime();return`<article class="card"><div class="head"><div><span class="num">${x(p.full_name||p.billing_email||r.user_id)}</span><h2>Account deletion</h2></div><span class="pill">${ready?"Ready":"Recovery window"}</span></div><div class="meta">Requested ${new Date(r.requested_at).toLocaleString("en-ZA")} · Recovery until ${due.toLocaleString("en-ZA")}</div><div class="actions"><button class="btn danger" data-finalize="${r.id}" ${ready?"":"disabled"}>${ready?"Finalise deletion":"Wait until recovery ends"}</button></div></article>`}async function finalize(id){if(!confirm("Finalise this account deletion? This soft-deletes the customer's login and is irreversible."))return;q("#status").textContent="Finalising deletion…";const{error}=await c.functions.invoke("finalize-account-deletion",{body:{request_id:id}});q("#status").textContent=error?(error.message||"Could not finalise deletion."):"Account deletion finalised.";if(!error)load()}document.addEventListener("DOMContentLoaded",load)})();